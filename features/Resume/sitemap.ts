import { type SitemapItem } from '@/lib/sitemap';

const sitemap: SitemapItem = {
  id: 'resume',
  meta: {
    title: 'Resume',
  },
  title: 'Resume',
  url: '/resume',
} as SitemapItem;

export default sitemap;
