import { type SitemapItem } from '@/lib/sitemap';

const sitemap: SitemapItem<'resume'> = {
  id: 'resume',
  meta: {
    title: 'Resume',
  },
  title: 'Resume',
  url: '/resume',
};

export default sitemap;
